pipeline {
    
    agent any

    stages {

        stage("Build") {
            steps {
                echo 'Build docker image'
                sh 'pwd -P'
                sh 'ls -l'
            }
        }

    }
}